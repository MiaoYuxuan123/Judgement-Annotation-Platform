server:
  port: 8080
  tomcat:
    max-swallow-size: 50MB

spring:
  application:
    name: judgment-annotation-platform
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 300MB